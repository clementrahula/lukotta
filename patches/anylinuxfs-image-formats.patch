diff -ruN -x target -x Cargo.lock a/anylinuxfs/src/cmd_mount.rs b/anylinuxfs/src/cmd_mount.rs
--- a/anylinuxfs/src/cmd_mount.rs	2026-08-23 15:42:33
+++ b/anylinuxfs/src/cmd_mount.rs	2026-08-23 15:42:33
@@ -1,3 +1,6 @@
+// Modified on 2026-08-22 by Clement Rahula <lukotta@rahula.dev>: opened every encoded image format, not qcow2 alone.
+// SPDX-License-Identifier: GPL-3.0-or-later
+
 use anyhow::Context;
 use bstr::{BString, ByteSlice, ByteVec};
 use common_utils::{
@@ -316,7 +319,7 @@
         if !Path::new(image_path).exists() {
             anyhow::bail!("Image file not found: {}", image_path);
         }
-        if DiskFormat::from_path(image_path) == DiskFormat::Qcow2 {
+        if DiskFormat::from_path(image_path).is_encoded() {
             let partition_info = DevInfo::unprobed_image(image_path, Some(part_num))?;
             let disk = File::open(partition_info.rdisk())
                 .context("Failed to open image file")?
@@ -356,7 +359,7 @@
     let token_path = Path::new(token);
     if token_path.is_file() {
         // Whole image file (no partition spec)
-        let dev_info = if DiskFormat::from_path(token) == DiskFormat::Qcow2 {
+        let dev_info = if DiskFormat::from_path(token).is_encoded() {
             DevInfo::unprobed_image(token, None)?
         } else {
             DevInfo::pv(token, true)?
diff -ruN -x target -x Cargo.lock a/anylinuxfs/src/devinfo.rs b/anylinuxfs/src/devinfo.rs
--- a/anylinuxfs/src/devinfo.rs	2026-08-23 15:42:33
+++ b/anylinuxfs/src/devinfo.rs	2026-08-23 15:42:33
@@ -1,3 +1,6 @@
+// Modified on 2026-08-22 by Clement Rahula <lukotta@rahula.dev>: added the VMDK, VDI, VHD and VHDX disk formats.
+// SPDX-License-Identifier: GPL-3.0-or-later
+
 use std::os::fd::AsRawFd;
 use std::path::Path;
 
@@ -14,6 +17,10 @@
     #[default]
     Raw,
     Qcow2,
+    Vmdk,
+    Vdi,
+    Vhd,
+    Vhdx,
 }
 
 impl DiskFormat {
@@ -22,12 +29,37 @@
         match self {
             DiskFormat::Raw => 0,
             DiskFormat::Qcow2 => 1,
+            DiskFormat::Vmdk => 2,
+            DiskFormat::Vdi => 3,
+            DiskFormat::Vhd => 4,
+            DiskFormat::Vhdx => 5,
         }
     }
 
+    /// Whether the bytes need decoding rather than being read straight
+    /// through: everything but Raw.
+    pub fn is_encoded(self) -> bool {
+        self != DiskFormat::Raw
+    }
+
     pub fn from_path(path: impl AsRef<BStr>) -> Self {
         if path.as_ref().ends_with(b".qcow2") {
             DiskFormat::Qcow2
+        } else if path.as_ref().ends_with(b".vmdk") {
+            DiskFormat::Vmdk
+        } else if path.as_ref().ends_with(b".vhdx") {
+            // Kept above ".vhd" although `ends_with` does not require it: a path
+            // ending in ".vhdx" does not end in ".vhd". The order is what a
+            // looser match would need, and the two formats share nothing but
+            // four letters.
+            DiskFormat::Vhdx
+        } else if path.as_ref().ends_with(b".vdi") {
+            DiskFormat::Vdi
+        } else if path.as_ref().ends_with(b".vhd") {
+            // Both kinds: a fixed VHD is raw with a footer after it, which the
+            // driver reads as such, and a dynamic one is blocks listed by a
+            // table. Neither is read straight through.
+            DiskFormat::Vhd
         } else {
             DiskFormat::Raw
         }
diff -ruN -x target -x Cargo.lock a/anylinuxfs/src/diskutil/mod.rs b/anylinuxfs/src/diskutil/mod.rs
--- a/anylinuxfs/src/diskutil/mod.rs	2026-08-23 15:42:33
+++ b/anylinuxfs/src/diskutil/mod.rs	2026-08-23 15:42:33
@@ -1,3 +1,6 @@
+// Modified on 2026-08-22 by Clement Rahula <lukotta@rahula.dev>: opened every encoded image format, not qcow2 alone.
+// SPDX-License-Identifier: GPL-3.0-or-later
+
 use anyhow::Context;
 use common_utils::{PathExt, is_encrypted_fs, safe_print};
 use derive_more::{AddAssign, Deref};
@@ -563,7 +566,7 @@
                 .map(|n| n.to_string_lossy().into_owned())
                 .unwrap_or_else(|| path.to_string());
 
-            if DiskFormat::from_path(p.as_bytes()) == DiskFormat::Qcow2 {
+            if DiskFormat::from_path(p.as_bytes()).is_encoded() {
                 // It's a qcow2 image file — will be probed by get_lsblk_info below
                 qcow2_images.push(Qcow2ListImage {
                     dev_info: DevInfo::unprobed_image(p.as_bytes(), None)?,

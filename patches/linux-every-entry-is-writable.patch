Every entry on a drive the guest serves is readable and writable.

Modes and flags come off the disk and are whatever wrote them: WSL's $LXMOD on
NTFS, a Linux umask, a Windows read-only attribute, a 0700 folder copied from a
Mac, an immutable or append-only flag. They refused twice: the client judged
the mode it was handed, and a server acting as anyone but root was judged
against the mode stored.

inode_permission() grants everything a read-only mount does not refuse, and
neither ownership, the sticky bit nor immutable and append-only flags refuse a
change. vfs_getattr_nosec(), where stat, statx and nfsd read attributes for
every filesystem including FUSE, adds read and write for everyone, and search
on folders. Nothing is written to the disk: modes and flags stay as stored.

--- a/include/linux/fs.h
+++ b/include/linux/fs.h
@@ -2308,8 +2308,8 @@
 #define IS_I_VERSION(inode)	__IS_FLG(inode, SB_I_VERSION)
 
 #define IS_NOQUOTA(inode)	((inode)->i_flags & S_NOQUOTA)
-#define IS_APPEND(inode)	((inode)->i_flags & S_APPEND)
-#define IS_IMMUTABLE(inode)	((inode)->i_flags & S_IMMUTABLE)
+#define IS_APPEND(inode)	((void)(inode), 0)
+#define IS_IMMUTABLE(inode)	((void)(inode), 0)
 
 #ifdef CONFIG_FS_POSIX_ACL
 #define IS_POSIXACL(inode)	__IS_FLG(inode, SB_POSIXACL)
@@ -3650,10 +3650,7 @@
 static inline int check_sticky(struct mnt_idmap *idmap,
 			       struct inode *dir, struct inode *inode)
 {
-	if (!(dir->i_mode & S_ISVTX))
-		return 0;
-
-	return __check_sticky(idmap, dir, inode);
+	return 0;
 }
 
 static inline void inode_has_no_xattr(struct inode *inode)
--- a/fs/namei.c
+++ b/fs/namei.c
@@ -512,35 +512,7 @@
 {
 	int retval;
 
-	retval = sb_permission(inode->i_sb, inode, mask);
-	if (retval)
-		return retval;
-
-	if (unlikely(mask & MAY_WRITE)) {
-		/*
-		 * Nobody gets write access to an immutable file.
-		 */
-		if (IS_IMMUTABLE(inode))
-			return -EPERM;
-
-		/*
-		 * Updating mtime will likely cause i_uid and i_gid to be
-		 * written back improperly if their true value is unknown
-		 * to the vfs.
-		 */
-		if (HAS_UNMAPPED_ID(idmap, inode))
-			return -EACCES;
-	}
-
-	retval = do_inode_permission(idmap, inode, mask);
-	if (retval)
-		return retval;
-
-	retval = devcgroup_inode_permission(inode, mask);
-	if (retval)
-		return retval;
-
-	return security_inode_permission(inode, mask);
+	return sb_permission(inode->i_sb, inode, mask);
 }
 EXPORT_SYMBOL(inode_permission);
 
--- a/fs/inode.c
+++ b/fs/inode.c
@@ -2524,17 +2524,7 @@
 bool inode_owner_or_capable(struct mnt_idmap *idmap,
 			    const struct inode *inode)
 {
-	vfsuid_t vfsuid;
-	struct user_namespace *ns;
-
-	vfsuid = i_uid_into_vfsuid(idmap, inode);
-	if (vfsuid_eq_kuid(vfsuid, current_fsuid()))
-		return true;
-
-	ns = current_user_ns();
-	if (vfsuid_has_mapping(ns, vfsuid) && ns_capable(ns, CAP_FOWNER))
-		return true;
-	return false;
+	return true;
 }
 EXPORT_SYMBOL(inode_owner_or_capable);
 
--- a/fs/stat.c
+++ b/fs/stat.c
@@ -162,12 +162,20 @@
 				  STATX_ATTR_DAX);
 
 	idmap = mnt_idmap(path->mnt);
-	if (inode->i_op->getattr)
-		return inode->i_op->getattr(idmap, path, stat,
-					    request_mask,
-					    query_flags | AT_GETATTR_NOSEC);
-
-	generic_fillattr(idmap, request_mask, inode, stat);
+	if (inode->i_op->getattr) {
+		int err = inode->i_op->getattr(idmap, path, stat,
+					       request_mask,
+					       query_flags | AT_GETATTR_NOSEC);
+		if (err)
+			return err;
+	} else {
+		generic_fillattr(idmap, request_mask, inode, stat);
+	}
+
+	if (S_ISDIR(stat->mode))
+		stat->mode |= 0777;
+	else if (S_ISREG(stat->mode))
+		stat->mode |= 0666;
 	return 0;
 }
 EXPORT_SYMBOL(vfs_getattr_nosec);

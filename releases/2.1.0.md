- Serve a filesystem from a process instead of the kernel.
- Ship the filesystem extension inside the v2 app.
- Give a drive a trash, so deleting is a rename.
- Format what swift-format asked for.
- Put the filesystem where it can be tested.
- Answer xattrs, so the volume stops collecting sidecars.
- Say what the module does not need asking about.
- Let the extension serve somewhere real, not only memory.
- Put one seam between the volume and whatever holds the files.
- Write the volume once, over the seam.
- Never let the extension be the reason a drive will not open.
- Read what mounting through the extension came to.
- Take the NFS fallback back out of v2.
- Put the extension back when an update has taken it away.
- Repair the extension at launch, and only when it has gone.
- Tell the extension things through its mount options, not the environment.
- Identify what the extension has been handed.
- Claim only what macOS cannot open.
- Do not ask the kernel to offload I/O that has nowhere to come from.
- Do not ask the volume who may open a file.
- Say the volume has nothing to let go of.
- Refuse to rename the drive, and reserve space before a big write.
- Check that reserving space grows a file and never shortens one.
- Work out which blocks a read or write touches.
- Read where things are on an NTFS volume.
- Read a real NTFS volume, not only sectors we wrote ourselves.
- Repair a file record before believing any of it.
- Read a real master file table record, not only one we wrote.
- Walk a record's attributes without walking off the end of it.
- Decode where a file's contents actually sit on the disk.
- Check the runlist against something the volume already told us.
- Show a file under the name somebody gave it.
- Find a record through the table's own runs, not by multiplying.
- Reach the root directory by number, on a real volume.
- Read a directory node without losing its end marker.
- Actually check that a marker's name bytes are ignored.
- List a real NTFS directory, through every layer.
- Work out what to read for a file, and never read past its end.
- Read a file's contents back off a real NTFS volume.
- Put the whole NTFS reader behind one object.
- Serve a real NTFS volume through the seam the extension uses.
- Read NTFS off the device the extension is handed.
- Show when a file was actually made.
- Do not show a drive's insides to whoever opens it.
- Find a name by the order the directory is already in.
- Look a name up by descending the tree, not by listing the folder.
- Measure what the NTFS read path costs.
- Measure streaming a large file through the runlist.
- Refuse a compressed or encrypted file rather than guessing at it.
- Do not report a fragmented file as an empty one.
- Check the arithmetic that decides which files a folder shows.
- Check a folder of five thousand comes back whole.
- Decrypt the way a disk is encrypted.
- Read an encrypted volume as though it were not.
- Refuse an encrypted drive rather than serving its ciphertext.
- Read which clusters a volume is using.
- Do not trap when the first cluster of a volume is free.
- Refuse a cluster number that would trap the arithmetic.
- Know whether a volume is in a state anybody should write to.
- Carry the writable answer where a write path would look for it.
- Put the drive's own name on the drive.
- Report how much of the drive is actually used.
- Let a real disk say how large it is.
- Work out where a write lands, without making one.
- Write to an NTFS volume, and have Linux read it back.
- Let the volume write, when it has both the means and the permission.
- Carry a write from the mount down to the device.
- Make the reader safe to call from every queue at once.
- Take out a lock that was not protecting anything, and add one that must.
- Claim and release clusters, refusing the two ways it goes wrong.
- Put a record back into the form the disk keeps it in.
- Check the fixup on a record that actually changed.
- Choose which clusters a file takes, all of them or none.
- Run the allocator against a real volume's free space.
- Pack a runlist back into the form NTFS keeps it in.
- Refuse a volume whose journal has work outstanding.
- Mark the volume dirty while we hold it.
- Format.
- A record goes back to every place it lives.
- Mark the volume before the first write, clear it on release.
- A new file takes a record nobody else has.
- Compose the record that is a new file.
- Order names the way the volume orders them.
- Look names up with the volume's table, not the language's.
- Put a name into the directory where it belongs.
- Make a file on an NTFS volume.
- Take a file away.
- Start the record search where the last one ended.
- Cut a full node in two.
- Grow the directory tree, and take names out of any level of it.
- Name a variable properly.
- Fill and empty the same directory thirty times.
- Name two loop variables properly.
- Walk the directory once, on bytes.
- Delete the splices nothing calls any more.

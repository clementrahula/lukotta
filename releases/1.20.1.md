- A drive opened through the background helper works. The helper could not find
  the Linux engine once macOS had installed it, so every drive opened that way
  stopped at "the mounting engine is missing" — which named the one thing that
  was not wrong with it.

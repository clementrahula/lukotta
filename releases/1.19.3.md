- Unlocking a drive that needs an administrator asks for the password once,
  under the app's own name, instead of twice — once for the drive and again a
  moment later for the background helper.
- The helper is set up before the drive is opened rather than after, so every
  later unlock needs nothing at all.

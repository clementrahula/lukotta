- The background helper is set up once and never asks again. A newer version of
  it replaces the running one by itself, so a fix to the helper arrives with an
  ordinary update rather than another password.
- A drive that needs an administrator is opened through the helper or not at
  all. It no longer falls back to a password panel named after a scripting tool.
- Update dialogs now appear in every language the app is translated into.
  Sparkle carried translations for 35 of the 37 and every one of those was
  partial, so a reader with the whole app in Arabic or Finnish met two thirds of
  the update dialogs in English. All 36 are complete, and five strings that were
  wrong upstream are corrected, among them a Finnish typo and an Arabic line cut
  off mid-word.
- Rescan says what it is doing. It used to finish too fast to see and then sit
  there, which read as a button that did nothing.

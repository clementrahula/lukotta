- Check that what cannot be done is refused cleanly.
- Let a file grow, so something can be copied onto the drive.
- Check the three sizes Windows reads and this one does not.
- Stop doing the same work on every megabyte.
- Make files in small directories too.
- Make folders, and put files in them.
- Remove an empty directory, and keep a removed file's bytes.
- Rename a file, and stop listing every written file as empty.
- Keep the copy of the size that a listing actually reads.
- Say when a file was last written to.
- Take the dirty flag off when the mount ends.
- Do not destroy a file to accomplish nothing.
- Cut a file down, and stop handing over the last file's bytes.
- Say the filesystem keeps no xattrs, not that one is missing.
- Stop sweeping a whole directory once per page.
- Ask for a page of a directory, not all of it.
- Report the volume's own block, and correct what the code claims.
